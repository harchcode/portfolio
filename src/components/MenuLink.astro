---
type Props = {
  id: string;
  label: string;
};

const { id, label } = Astro.props;
---

<a
  href={`#${id}`}
  class={`block text-black bg-indigo-100 px-2 py-1 border-2 border-black rounded-full w-20 text-center`}
  >{label}</a
>
